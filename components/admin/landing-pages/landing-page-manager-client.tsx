'use client'

import { useMemo, useState } from 'react'
import { ChevronDown, ChevronUp, Copy, Eye, FilePlus2, Globe2, Pencil, Plus, Save, Trash2 } from 'lucide-react'
import { createLandingPage, deleteLandingPage, duplicateLandingPage, setLandingPageStatus, updateLandingPage, uploadLandingPageMediaAction } from '@/lib/admin/landing-page-actions'
import type { LandingPage, LandingPageInput, LandingSection } from '@/lib/landing-pages/types'
import type { StorefrontProduct } from '@/lib/services/storefront-utils'

const blankSections: LandingSection[] = [
  { id: 'hero-1', type: 'hero', eyebrow: 'SahiGadget', title: 'পণ্যের প্রধান প্রতিশ্রুতি লিখুন', body: 'পণ্য বা প্রচারণার মূল কথা এক সংক্ষিপ্ত অনুচ্ছেদে লিখুন।', ctaLabel: 'পণ্য দেখুন', ctaHref: '#product' }, { id: 'benefits-1', type: 'benefits', title: 'কেন গ্রাহকরা এটি পছন্দ করেন', items: [{ title: 'আসল পণ্যের তথ্য', body: 'শুধু যাচাই করা পণ্যের তথ্য ব্যবহার করুন।' }, { title: 'সঠিক মূল্য ও প্রাপ্যতা', body: 'মূল্য ও প্রাপ্যতা নির্বাচিত পণ্যের তথ্য অনুযায়ী দেখানো হবে।' }] },
]

const templates: Array<{ label: string; sections: LandingSection[] }> = [
  { label: 'পণ্য বিক্রয় পৃষ্ঠা', sections: [{ id: 'hero-1', type: 'hero', eyebrow: 'SahiGadget', title: 'পণ্যের জন্য একটি আকর্ষণীয় প্রচারণা', body: 'পণ্যের স্পষ্ট সুবিধা ও যাচাই করা তথ্য দিয়ে শুরু করুন।', ctaLabel: 'অর্ডার করুন', ctaHref: '#order' }, { id: 'gallery-1', type: 'product_gallery', title: 'পণ্যটি কাছ থেকে দেখুন' }, { id: 'benefits-1', type: 'benefits', title: 'দৈনন্দিন ব্যবহারের জন্য উপযোগী', items: [{ title: 'যাচাই করা পণ্যের তথ্য', body: 'বর্তমান পণ্যের তথ্য অনুযায়ী দেখানো হবে।' }, { title: 'সঠিক প্রাপ্যতা', body: 'নির্বাচিত ভ্যারিয়েন্টের প্রাপ্যতা দেখানো হবে।' }] }, { id: 'order-1', type: 'order', title: 'অর্ডার করতে প্রস্তুত?' }] },
  { label: 'বিশেষ অফার পৃষ্ঠা', sections: [{ id: 'hero-1', type: 'hero', title: 'সময়সীমার প্রচারণা বার্তা', body: 'শুধু সত্য প্রচারণার সময়সীমা ও যাচাই করা মূল্য ব্যবহার করুন।', ctaLabel: 'অফার দেখুন', ctaHref: '#offer' }, { id: 'offer-1', type: 'offer', title: 'বর্তমান পণ্যের অফার', discountText: 'অনুমোদিত প্রচারণার লেখা যোগ করুন', showLivePrice: true }, { id: 'countdown-1', type: 'countdown', title: 'প্রচারণা শেষ হবে', endsAt: new Date(Date.now() + 86400000).toISOString() }, { id: 'order-1', type: 'order', title: 'SahiGadget-এর মাধ্যমে অর্ডার করুন' }] },
  { label: 'পণ্য + ক্যাশ অন ডেলিভারি পৃষ্ঠা', sections: [{ id: 'hero-1', type: 'hero', imageUrl: 'https://ncknpaezdhsqiicdjtgr.supabase.co/storage/v1/object/public/landing-pages/pages/00000000-0000-0000-0000-000000000001-1786900985869-miw4d63.webp', title: 'Samsung Guru Music 2', body: 'অরিজিনাল মেইড ইন ভিয়েতনামের গ্লোবাল ভার্সন ফোন।', ctaLabel: 'অর্ডার করুন', ctaHref: '#order' }, { id: 'features-1', type: 'features', title: 'পণ্যের প্রধান বৈশিষ্ট্য', items: [{ title: '১ বছরের ওয়ারেন্টি', body: 'নির্ধারিত ওয়ারেন্টি নীতির আওতায় সহায়তা।' }, { title: 'ফোনটি ২ সিম সাপোর্টেড', body: 'একসাথে ২টি সিম ব্যবহার করা যাবে।' }, { title: '১ চার্জে চলবে ৩ দিন', body: 'ব্যবহারভেদে ৮০০ mAh ব্যাটারির দীর্ঘ ব্যাকআপ।' }, { title: 'মেমোরি কার্ড ও ভিডিও প্লেয়ার সাপোর্টেড', body: 'প্রয়োজনীয় মিডিয়া ব্যবহারের সুবিধা।' }] }, { id: 'variant-1', type: 'variant_selector', title: 'রং নির্বাচন করুন' }, { id: 'quantity-1', type: 'quantity_selector', title: 'পরিমাণ' }, { id: 'delivery-1', type: 'delivery_info', title: 'ডেলিভারি ও পেমেন্ট', body: 'ক্যাশ অন ডেলিভারি। পণ্য হাতে পেয়ে মূল্য পরিশোধ করুন।' }, { id: 'order-1', type: 'order', title: 'অর্ডার করুন' }, { id: 'specifications-1', type: 'specifications', title: 'পণ্যের বিস্তারিত', fields: [{ label: 'ব্র্যান্ড', value: 'Samsung' }, { label: 'মডেল', value: 'Guru Music 2' }, { label: 'সিম', value: 'Dual SIM' }, { label: 'ডিসপ্লে', value: '2.0 inch' }, { label: 'সাউন্ড', value: 'Loudspeaker, Headphone Jack' }, { label: 'ব্যাটারি ক্যাপাসিটি', value: '800 mAh (Lithium Ion)' }, { label: 'ব্যাটারি টাইপ', value: 'Removable' }, { label: 'এফএম রেডিও', value: 'Yes' }, { label: 'ফ্ল্যাশ', value: 'LED flash' }, { label: 'মেড বাই', value: 'Vietnam' }] }, { id: 'reviews-1', type: 'social_proof', title: 'গ্রাহকের মতামত', body: 'বাস্তব গ্রাহকদের যাচাই করা মতামত।', reviews: [{ name: 'মো. রহিম', text: 'অসাধারণ ফোন, ব্যাটারি লাইফ ভালো ২/৩ দিন চলে', enabled: true, sortOrder: 0 }, { name: 'Fatima', text: 'ক্লাসিক ডিজাইন, খুব পছন্দ হয়েছে।', enabled: true, sortOrder: 1 }] }, { id: 'faq-1', type: 'faq', title: 'সাধারণ জিজ্ঞাসা', items: [{ question: 'এটা কি অরিজিনাল?', answer: 'হ্যাঁ, এটা অরিজিনাল মেইড ইন ভিয়েতনামের গ্লোবাল ভার্সন ফোন।', enabled: true, sortOrder: 0 }, { question: 'ব্যাটারি এবং সিম?', answer: '৮০০ mAh ব্যাটারি শুধু কথা বললে ২/৩ দিন চার্জিং ব্যাকআপ দিবে এবং ২টি (সিম) ব্যবহার করা যাবে।', enabled: true, sortOrder: 1 }, { question: 'অর্ডার দিলে কবে পাওয়া যাবে?', answer: '২/৩ দিনের মধ্যেই পেয়ে যাবেন, ইনশাআল্লাহ।', enabled: true, sortOrder: 2 }, { question: 'অগ্রিম টাকা দেওয়া লাগবে?', answer: 'জ্বী না স্যার, ১ টাকাও অগ্রিম দিতে হবে না। আপনার প্রোডাক্ট হাতে পাওয়ার পর দেখে তারপর ডেলিভারি ম্যানকে টাকা পরিশোধ করবেন।', enabled: true, sortOrder: 3 }] }, { id: 'cta-1', type: 'cta', title: 'অর্ডার করতে প্রস্তুত?', body: 'আপনার পছন্দের রং নির্বাচন করে অর্ডার সম্পন্ন করুন।', label: 'অর্ডার করুন', href: '#order' }] },
  { label: 'প্রচারণা পৃষ্ঠা', sections: [{ id: 'announcement-1', type: 'announcement', eyebrow: 'প্রচারণা', title: 'স্পষ্ট প্রচারণা আপডেট', body: 'প্রচারণার লেখা নির্ভুল ও সংক্ষিপ্ত রাখুন।', tone: 'emerald' }, { id: 'features-1', type: 'features', title: 'প্রচারণার মূল দিক', items: [{ title: 'যাচাই করা একটি সুবিধা যোগ করুন', body: 'শুধু যাচাই করা স্টোরের তথ্য ব্যবহার করুন।' }] }, { id: 'faq-1', type: 'faq', title: 'সাধারণ জিজ্ঞাসা', items: [{ question: 'কী তথ্য যোগ করব?', answer: 'শুধু অনুমোদিত পণ্য, নীতি ও প্রচারণার তথ্য ব্যবহার করুন।' }] }, { id: 'cta-1', type: 'cta', title: 'SahiGadget দেখুন', body: 'বর্তমান পণ্যের তালিকায় যান।', label: 'পণ্য দেখুন', href: '/products' }] },
]

function blankInput(): LandingPageInput {
  return { slug: '', internal_name: '', page_type: 'product', status: 'draft', linked_product_id: '', hero_image_url: '', mobile_hero_image_url: '', og_image_url: '', sections: blankSections, seo_title: '', seo_description: '', noindex: true, starts_at: null, ends_at: null, linked_product_ids: [] }
}

function parseSections(value: string) {
  const sections = JSON.parse(value) as LandingSection[]
  if (!Array.isArray(sections)) throw new Error('Sections must be a JSON array.')
  return sections
}

function nextSectionId(sections: LandingSection[], type: LandingSection['type']) {
  const prefix = `${type}-`
  const used = new Set(sections.map((section) => section.id))
  let index = 1
  while (used.has(`${prefix}${index}`)) index += 1
  return `${prefix}${index}`
}

function sectionTemplate(type: LandingSection['type'], sections: LandingSection[]): LandingSection {
  const id = nextSectionId(sections, type)
  if (type === 'hero') return { id, type, title: 'নতুন প্রচারণার শিরোনাম', body: 'সংক্ষিপ্ত ও যাচাই করা প্রচারণার বিবরণ যোগ করুন।', ctaLabel: 'দেখুন', ctaHref: '/products' }
  if (type === 'announcement') return { id, type, title: 'নতুন ঘোষণা', body: 'অনুমোদিত স্টোর বা প্রচারণার তথ্য যোগ করুন।', tone: 'neutral' }
  if (type === 'features' || type === 'benefits') return { id, type, title: type === 'features' ? 'মূল বৈশিষ্ট্য' : 'সুবিধা', items: [{ title: 'নতুন বিষয়', body: 'যাচাই করা একটি সুবিধা বর্ণনা করুন।' }] }
  if (type === 'product_gallery') return { id, type, title: 'পণ্যের গ্যালারি' }
  if (type === 'offer') return { id, type, title: 'বর্তমান অফার', discountText: 'শুধু অনুমোদিত প্রচারণার লেখা', showLivePrice: true }
  if (type === 'specifications') return { id, type, title: 'বিস্তারিত তথ্য', fields: [{ label: 'নতুন তথ্যের নাম', value: 'যাচাই করা মান ব্যবহার করুন' }] }
  if (type === 'variant_selector') return { id, type, title: 'ভ্যারিয়েন্ট বেছে নিন' }
  if (type === 'quantity_selector') return { id, type, title: 'পরিমাণ' }
  if (type === 'delivery_info') return { id, type, title: 'ডেলিভারি তথ্য', body: 'বর্তমান স্টোর সেটিংস ও নীতি ব্যবহার করুন।' }
  if (type === 'warranty') return { id, type, title: 'ওয়ারেন্টি ও আস্থা', items: ['শুধু নির্ধারিত নীতির বক্তব্য যোগ করুন।'] }
  if (type === 'social_proof') return { id, type, title: 'গ্রাহকের মতামত', body: 'শুধু বাস্তব ও অনুমোদিত গ্রাহক কনটেন্ট যোগ করুন।', disclaimer: 'শুধু সরবরাহ করা কনটেন্ট ব্যবহার করুন; বানানো রিভিউ নয়।' }
  if (type === 'image_text') return { id, type, title: 'ছবি ও লেখা', body: 'সহায়ক কনটেন্ট যোগ করুন।', imageUrl: '', imageAlt: 'ছবিটির বর্ণনা লিখুন' }
  if (type === 'comparison') return { id, type, title: 'তুলনা', items: [{ label: 'যাচাই করা বৈশিষ্ট্য', value: 'যাচাই করা মান' }] }
  if (type === 'related_products') return { id, type, title: 'সম্পর্কিত পণ্য', productIds: [] }
  if (type === 'sticky_mobile_cta') return { id, type, label: 'অর্ডার করুন', href: '#order' }
  if (type === 'countdown') return { id, type, title: 'প্রচারণা শেষ হবে', endsAt: new Date(Date.now() + 86400000).toISOString() }
  if (type === 'order') return { id, type, title: 'অর্ডার করতে প্রস্তুত?', body: 'SahiGadget-এর অর্ডার প্রক্রিয়া ব্যবহার করুন।', ctaLabel: 'অর্ডার করুন' }
  if (type === 'trust') return { id, type, title: 'আস্থার তথ্য', items: ['যাচাই করা আস্থার বক্তব্য যোগ করুন।'] }
  if (type === 'faq') return { id, type, title: 'সাধারণ জিজ্ঞাসা', items: [{ question: 'নতুন প্রশ্ন', answer: 'যাচাই করা উত্তর যোগ করুন।' }] }
  if (type === 'rich_text') return { id, type, title: 'সহায়ক তথ্য', body: 'যাচাই করা সহায়ক কনটেন্ট যোগ করুন।' }
  return { id, type: 'cta', title: 'আরও পণ্য দেখুন', body: 'পণ্যের তালিকা দেখুন।', label: 'পণ্য দেখুন', href: '/products' }
}

export function LandingPageManagerClient({ initialPages, products }: { initialPages: LandingPage[]; products: StorefrontProduct[] }) {
  const [pages] = useState(initialPages)
  const [editingId, setEditingId] = useState<string | null>(null)
  const [form, setForm] = useState<LandingPageInput>(blankInput())
  const [sectionsText, setSectionsText] = useState(JSON.stringify(blankSections, null, 2))
  const [feedback, setFeedback] = useState('')
  const [busy, setBusy] = useState(false)
  const [uploading, setUploading] = useState(false)
  const editingPage = useMemo(() => pages.find((page) => page.id === editingId) ?? null, [editingId, pages])

  function setSections(next: LandingSection[]) { setForm((current) => ({ ...current, sections: next })); setSectionsText(JSON.stringify(next, null, 2)) }
  function currentSections() { return parseSections(sectionsText) }
  function startCreate() { setEditingId(null); const next = blankInput(); setForm(next); setSectionsText(JSON.stringify(next.sections, null, 2)); setFeedback('') }
  function startEdit(page: LandingPage) { const next: LandingPageInput = { slug: page.slug, internal_name: page.internal_name, page_type: page.page_type, status: page.status, linked_product_id: page.linked_product_id, hero_image_url: page.hero_image_url, mobile_hero_image_url: page.mobile_hero_image_url, og_image_url: page.og_image_url, sections: page.sections, seo_title: page.seo_title, seo_description: page.seo_description, noindex: page.noindex, starts_at: page.starts_at, ends_at: page.ends_at, linked_product_ids: page.linked_products?.map((item) => item.product_id) ?? [] }; setEditingId(page.id); setForm(next); setSectionsText(JSON.stringify(next.sections, null, 2)); setFeedback('') }
  function addSection(type: LandingSection['type']) { setSections([...currentSections(), sectionTemplate(type, currentSections())]) }
  function updateSection(index: number, patch: Partial<LandingSection>) { const sections = currentSections(); sections[index] = { ...sections[index], ...patch } as LandingSection; setSections(sections) }
  function updateReview(sectionIndex: number, reviewIndex: number, patch: Partial<NonNullable<Extract<LandingSection, { type: 'social_proof' }>['reviews']>[number]>) { const section = currentSections()[sectionIndex]; if (section.type !== 'social_proof') return; const reviews = [...(section.reviews ?? [])]; reviews[reviewIndex] = { ...reviews[reviewIndex], ...patch }; updateSection(sectionIndex, { reviews }) }
  function addReview(sectionIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'social_proof') return; updateSection(sectionIndex, { reviews: [...(section.reviews ?? []), { name: '', text: '', enabled: true, sortOrder: section.reviews?.length ?? 0 }] }) }
  function removeReview(sectionIndex: number, reviewIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'social_proof') return; updateSection(sectionIndex, { reviews: (section.reviews ?? []).filter((_, index) => index !== reviewIndex) }) }
  function updateFeature(sectionIndex: number, itemIndex: number, patch: Partial<{ title: string; body?: string; icon?: string; enabled?: boolean }>) { const section = currentSections()[sectionIndex]; if (section.type !== 'features' && section.type !== 'benefits') return; const items = [...section.items]; items[itemIndex] = { ...items[itemIndex], ...patch }; updateSection(sectionIndex, { items }) }
  function addFeature(sectionIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'features' && section.type !== 'benefits') return; updateSection(sectionIndex, { items: [...section.items, { title: '', body: '', enabled: true }] }) }
  function removeFeature(sectionIndex: number, itemIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'features' && section.type !== 'benefits') return; updateSection(sectionIndex, { items: section.items.filter((_, index) => index !== itemIndex) }) }
  function moveFeature(sectionIndex: number, itemIndex: number, direction: -1 | 1) { const section = currentSections()[sectionIndex]; if (section.type !== 'features' && section.type !== 'benefits') return; const target = itemIndex + direction; if (target < 0 || target >= section.items.length) return; const items = [...section.items]; [items[itemIndex], items[target]] = [items[target], items[itemIndex]]; updateSection(sectionIndex, { items }) }
  function updateSpecification(sectionIndex: number, fieldIndex: number, patch: Partial<{ label: string; value: string; enabled?: boolean }>) { const section = currentSections()[sectionIndex]; if (section.type !== 'specifications') return; const fields = [...section.fields]; fields[fieldIndex] = { ...fields[fieldIndex], ...patch }; updateSection(sectionIndex, { fields }) }
  function addSpecification(sectionIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'specifications') return; updateSection(sectionIndex, { fields: [...section.fields, { label: '', value: '', enabled: true }] }) }
  function removeSpecification(sectionIndex: number, fieldIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'specifications') return; updateSection(sectionIndex, { fields: section.fields.filter((_, index) => index !== fieldIndex) }) }
  function moveSpecification(sectionIndex: number, fieldIndex: number, direction: -1 | 1) { const section = currentSections()[sectionIndex]; if (section.type !== 'specifications') return; const target = fieldIndex + direction; if (target < 0 || target >= section.fields.length) return; const fields = [...section.fields]; [fields[fieldIndex], fields[target]] = [fields[target], fields[fieldIndex]]; updateSection(sectionIndex, { fields }) }
  function updateFaq(sectionIndex: number, itemIndex: number, patch: Partial<NonNullable<Extract<LandingSection, { type: 'faq' }>['items']>[number]>) { const section = currentSections()[sectionIndex]; if (section.type !== 'faq') return; const items = [...section.items]; items[itemIndex] = { ...items[itemIndex], ...patch }; updateSection(sectionIndex, { items }) }
  function addFaq(sectionIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'faq') return; updateSection(sectionIndex, { items: [...section.items, { question: '', answer: '', enabled: true, sortOrder: section.items.length }] }) }
  function removeFaq(sectionIndex: number, itemIndex: number) { const section = currentSections()[sectionIndex]; if (section.type !== 'faq') return; updateSection(sectionIndex, { items: section.items.filter((_, index) => index !== itemIndex) }) }
  function duplicateSection(index: number) { const sections = currentSections(); const original = sections[index]; const copy = { ...original, id: nextSectionId(sections, original.type) } as LandingSection; sections.splice(index + 1, 0, copy); setSections(sections) }
  function moveSection(index: number, direction: -1 | 1) { const sections = currentSections(); const target = index + direction; if (target < 0 || target >= sections.length) return; [sections[index], sections[target]] = [sections[target], sections[index]]; setSections(sections) }
  function removeSection(index: number) { const sections = currentSections(); sections.splice(index, 1); setSections(sections) }
  async function uploadMedia(file: File, target: 'hero_image_url' | 'mobile_hero_image_url' | 'og_image_url') { setUploading(true); setFeedback(''); try { const data = new FormData(); data.set('file', file); const result = await uploadLandingPageMediaAction(data); if (!result.ok || !result.url) { setFeedback(result.message); return } setForm((current) => ({ ...current, [target]: result.url })); setFeedback('Image uploaded. Save the page to apply it.'); } finally { setUploading(false) } }
  async function uploadReviewMedia(file: File, sectionIndex: number, reviewIndex: number) { setUploading(true); setFeedback(''); try { const data = new FormData(); data.set('file', file); const result = await uploadLandingPageMediaAction(data); if (!result.ok || !result.url) { setFeedback(result.message); return } updateReview(sectionIndex, reviewIndex, { imageUrl: result.url, imageEnabled: true }); setFeedback('Review image uploaded. Save the page to apply it.'); } finally { setUploading(false) } }
  async function submit() { setBusy(true); setFeedback(''); try { const sections = currentSections(); const payload = { ...form, sections, linked_product_ids: form.linked_product_id ? [form.linked_product_id, ...(form.linked_product_ids ?? []).filter((id) => id !== form.linked_product_id)] : form.linked_product_ids }; const result = editingId ? await updateLandingPage(editingId, payload) : await createLandingPage(payload); setFeedback(result.message); if (result.ok) window.location.reload() } catch (error) { setFeedback(error instanceof Error ? error.message : 'The landing page could not be saved.') } finally { setBusy(false) } }
  async function changeStatus(page: LandingPage, status: 'draft' | 'published' | 'archived') { setBusy(true); const result = await setLandingPageStatus(page.id, status); setFeedback(result.message); setBusy(false); if (result.ok) window.location.reload() }
  async function duplicate(page: LandingPage) { setBusy(true); const result = await duplicateLandingPage(page.id); setFeedback(result.message); setBusy(false); if (result.ok) window.location.reload() }
  async function remove(page: LandingPage) { if (!window.confirm(`Delete “${page.internal_name}”? This removes only the landing-page record.`)) return; setBusy(true); const result = await deleteLandingPage(page.id); setFeedback(result.message); setBusy(false); if (result.ok) window.location.reload() }

  return <div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_480px]">
    <section className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-emerald-700">Structured pages</p><h2 className="mt-1 text-xl font-semibold text-slate-950">Landing page library</h2><p className="mt-1 text-sm text-slate-500">Build campaigns without editing code. Product facts remain live from the catalogue.</p></div><button type="button" onClick={startCreate} className="inline-flex h-11 items-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-bold text-white hover:bg-emerald-700"><FilePlus2 className="h-4 w-4" /> New landing page</button></div>
      <div className="space-y-3">{pages.map((page) => <article key={page.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-base font-bold text-slate-950">{page.internal_name}</h3><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-slate-600">{page.status}</span><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-emerald-700">{page.page_type}</span></div><p className="mt-2 text-sm text-slate-500">/landing/{page.slug} · {page.sections.length} sections</p></div><div className="flex flex-wrap gap-2"><button type="button" onClick={() => startEdit(page)} className="inline-flex h-9 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-700 hover:border-emerald-300"><Pencil className="h-3.5 w-3.5" /> Edit</button><button type="button" disabled={busy} onClick={() => duplicate(page)} className="inline-flex h-9 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-700 hover:border-emerald-300"><Copy className="h-3.5 w-3.5" /> Duplicate</button>{page.status === 'published' ? <button type="button" disabled={busy} onClick={() => changeStatus(page, 'draft')} className="inline-flex h-9 items-center gap-2 rounded-lg border border-amber-200 px-3 text-xs font-bold text-amber-700 hover:bg-amber-50">Unpublish</button> : <button type="button" disabled={busy} onClick={() => changeStatus(page, 'published')} className="inline-flex h-9 items-center gap-2 rounded-lg bg-emerald-700 px-3 text-xs font-bold text-white hover:bg-emerald-800"><Globe2 className="h-3.5 w-3.5" /> Publish</button>}<button type="button" disabled={busy} onClick={() => remove(page)} className="inline-flex h-9 items-center gap-2 rounded-lg border border-rose-200 px-3 text-xs font-bold text-rose-700 hover:bg-rose-50"><Trash2 className="h-3.5 w-3.5" /> Delete</button></div></div><div className="mt-4 flex flex-wrap gap-2 text-xs text-slate-500"><span>{page.noindex ? 'No index' : 'Indexable'}</span>{page.starts_at ? <span>Starts {new Date(page.starts_at).toLocaleString()}</span> : null}{page.ends_at ? <span>Ends {new Date(page.ends_at).toLocaleString()}</span> : null}{page.status === 'published' && !page.noindex ? <a className="font-bold text-emerald-700 hover:underline" href={`/landing/${page.slug}`} target="_blank" rel="noreferrer"><Eye className="mr-1 inline h-3.5 w-3.5" /> Preview</a> : null}</div></article>)}{!pages.length ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><p className="font-bold text-slate-800">No landing pages yet.</p><p className="mt-2 text-sm text-slate-500">Start from a template and link a current product.</p></div> : null}</div>
    </section>
    <section className="rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm xl:sticky xl:top-24 xl:self-start"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-emerald-700">Visual builder</p><h2 className="mt-1 text-xl font-semibold text-slate-950">{editingPage ? 'Edit landing page' : 'Create landing page'}</h2></div><button type="button" onClick={startCreate} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-900" aria-label="Clear builder"><Plus className="h-4 w-4" /></button></div>
      <div className="mt-5 space-y-4"><div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><p className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Start from a template</p><div className="mt-2 grid gap-2 sm:grid-cols-2">{templates.map((template) => <button key={template.label} type="button" onClick={() => setSections(template.sections.map((section) => ({ ...section })))} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-left text-xs font-bold text-slate-700 hover:border-emerald-400 hover:text-emerald-700">{template.label}</button>)}</div></div>
        <label className="block text-sm font-semibold text-slate-700">Internal name<input value={form.internal_name} onChange={(e) => setForm({ ...form, internal_name: e.target.value })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100" placeholder="Samsung Guru Music 2 campaign" /></label><label className="block text-sm font-semibold text-slate-700">URL slug<input value={form.slug} onChange={(e) => setForm({ ...form, slug: e.target.value })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100" placeholder="samsung-guru-music-2" /></label><div className="grid gap-3 sm:grid-cols-2"><label className="block text-sm font-semibold text-slate-700">Page type<select value={form.page_type} onChange={(e) => setForm({ ...form, page_type: e.target.value as LandingPageInput['page_type'] })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm"><option value="product">Product</option><option value="campaign">Campaign</option></select></label><label className="block text-sm font-semibold text-slate-700">Linked product<select value={form.linked_product_id ?? ''} onChange={(e) => setForm({ ...form, linked_product_id: e.target.value || null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm"><option value="">No single product</option>{products.map((product) => <option key={product.id} value={product.id}>{product.name}</option>)}</select></label></div><label className="block text-sm font-semibold text-slate-700">Main landing image URL<input value={form.hero_image_url ?? ''} onChange={(e) => setForm({ ...form, hero_image_url: e.target.value || null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm" placeholder="Use an approved HTTPS image URL" /><span className="mt-1 block text-xs font-normal text-slate-500">This image stays fixed while customers change variants.</span><input type="file" accept="image/jpeg,image/png,image/webp,image/avif" disabled={uploading} onChange={(e) => { const file = e.target.files?.[0]; if (file) void uploadMedia(file, 'hero_image_url'); e.currentTarget.value = '' }} className="mt-2 block w-full text-xs" /></label><label className="block text-sm font-semibold text-slate-700">Mobile landing image URL<input value={form.mobile_hero_image_url ?? ''} onChange={(e) => setForm({ ...form, mobile_hero_image_url: e.target.value || null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm" placeholder="Optional responsive image" /><input type="file" accept="image/jpeg,image/png,image/webp,image/avif" disabled={uploading} onChange={(e) => { const file = e.target.files?.[0]; if (file) void uploadMedia(file, 'mobile_hero_image_url'); e.currentTarget.value = '' }} className="mt-2 block w-full text-xs" /></label><label className="block text-sm font-semibold text-slate-700">Social preview image URL<input value={form.og_image_url ?? ''} onChange={(e) => setForm({ ...form, og_image_url: e.target.value || null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm" placeholder="Optional HTTPS image for social sharing" /><input type="file" accept="image/jpeg,image/png,image/webp,image/avif" disabled={uploading} onChange={(e) => { const file = e.target.files?.[0]; if (file) void uploadMedia(file, 'og_image_url'); e.currentTarget.value = '' }} className="mt-2 block w-full text-xs" /></label><div className="grid gap-3 sm:grid-cols-2"><label className="block text-sm font-semibold text-slate-700">Starts at<input type="datetime-local" value={form.starts_at ? form.starts_at.slice(0, 16) : ''} onChange={(e) => setForm({ ...form, starts_at: e.target.value ? new Date(e.target.value).toISOString() : null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm" /></label><label className="block text-sm font-semibold text-slate-700">Ends at<input type="datetime-local" value={form.ends_at ? form.ends_at.slice(0, 16) : ''} onChange={(e) => setForm({ ...form, ends_at: e.target.value ? new Date(e.target.value).toISOString() : null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm" /></label></div><label className="block text-sm font-semibold text-slate-700">SEO title<input value={form.seo_title ?? ''} onChange={(e) => setForm({ ...form, seo_title: e.target.value || null })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm" /></label><label className="block text-sm font-semibold text-slate-700">SEO description<textarea value={form.seo_description ?? ''} onChange={(e) => setForm({ ...form, seo_description: e.target.value || null })} rows={3} className="mt-1 w-full rounded-lg border border-slate-200 p-3 text-sm" /></label><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={form.noindex} onChange={(e) => setForm({ ...form, noindex: e.target.checked })} /> Keep this page out of search engines while drafting</label>
        <div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-bold text-slate-900">Sections</p><p className="text-xs text-slate-500">Add, reorder, duplicate, disable, or remove sections.</p></div><span className="rounded-full bg-white px-2 py-1 text-xs font-bold text-slate-600">{form.sections.length}</span></div><div className="mt-3 space-y-2">{form.sections.map((section, index) => <div key={section.id} className="rounded-lg border border-slate-200 bg-white p-3"><div className="flex items-start gap-2"><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-slate-900">{index + 1}. {section.type.replaceAll('_', ' ')}</p><p className="mt-1 text-xs text-slate-500">{section.id}{section.enabled === false ? ' · disabled' : ''}</p></div><button type="button" onClick={() => updateSection(index, { enabled: section.enabled === false })} className="rounded p-1.5 text-xs font-bold text-slate-500 hover:bg-slate-100" aria-label={`${section.enabled === false ? 'Enable' : 'Disable'} section`}>{section.enabled === false ? 'Enable' : 'Disable'}</button></div>{section.type === 'variant_selector' ? <div className="mt-3 rounded-lg bg-slate-50 p-3"><p className="text-xs font-bold uppercase tracking-[0.12em] text-slate-500">Variant thumbnail media</p>{(products.find((item) => item.id === form.linked_product_id)?.variants ?? []).map((variant) => <label key={variant.id} className="mt-2 block text-xs font-semibold text-slate-700">{variant.variant_title || variant.color || 'Variant'}<input value={section.variantImages?.[variant.id] ?? ''} onChange={(e) => { const variantImages = { ...(section.variantImages ?? {}) }; if (e.target.value) variantImages[variant.id] = e.target.value; else delete variantImages[variant.id]; updateSection(index, { variantImages }) }} className="mt-1 h-9 w-full rounded-lg border border-slate-200 bg-white px-2 text-xs" placeholder="Optional approved HTTPS image URL" /></label>)}</div> : null}{section.type === 'features' || section.type === 'benefits' ? <div className="mt-3 rounded-lg bg-slate-50 p-3"><div className="flex items-center justify-between gap-2"><p className="text-xs font-bold uppercase tracking-[0.12em] text-slate-500">{section.type === 'features' ? 'Product features' : 'Benefits'}</p><button type="button" onClick={() => addFeature(index)} className="rounded border border-emerald-200 px-2 py-1 text-xs font-bold text-emerald-700">Add item</button></div><div className="mt-3 space-y-2">{section.items.map((item, itemIndex) => <div key={`${section.id}-feature-${itemIndex}`} className="rounded-lg border border-slate-200 bg-white p-3"><label className="block text-xs font-semibold text-slate-700">Title<input value={item.title} onChange={(e) => updateFeature(index, itemIndex, { title: e.target.value })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label><label className="mt-2 block text-xs font-semibold text-slate-700">Description<textarea value={item.body ?? ''} onChange={(e) => updateFeature(index, itemIndex, { body: e.target.value })} rows={2} className="mt-1 w-full rounded border border-slate-200 px-2 py-2 text-xs" /></label><div className="mt-2 flex flex-wrap items-center gap-2"><button type="button" onClick={() => moveFeature(index, itemIndex, -1)} disabled={itemIndex === 0} className="rounded border border-slate-200 px-2 py-1 text-xs disabled:opacity-30">Up</button><button type="button" onClick={() => moveFeature(index, itemIndex, 1)} disabled={itemIndex === section.items.length - 1} className="rounded border border-slate-200 px-2 py-1 text-xs disabled:opacity-30">Down</button><label className="flex items-center gap-2 text-xs font-semibold"><input type="checkbox" checked={item.enabled !== false} onChange={(e) => updateFeature(index, itemIndex, { enabled: e.target.checked })} /> Enabled</label><button type="button" onClick={() => removeFeature(index, itemIndex)} className="rounded border border-rose-200 px-2 py-1 text-xs font-bold text-rose-700">Remove</button></div></div>)}</div></div> : null}{section.type === 'specifications' ? <div className="mt-3 rounded-lg bg-slate-50 p-3"><div className="flex items-center justify-between gap-2"><p className="text-xs font-bold uppercase tracking-[0.12em] text-slate-500">Product details</p><button type="button" onClick={() => addSpecification(index)} className="rounded border border-emerald-200 px-2 py-1 text-xs font-bold text-emerald-700">Add detail</button></div><div className="mt-3 space-y-2">{section.fields.map((field, fieldIndex) => <div key={`${section.id}-spec-${fieldIndex}`} className="rounded-lg border border-slate-200 bg-white p-3"><div className="grid gap-2 sm:grid-cols-2"><label className="text-xs font-semibold text-slate-700">Label<input value={field.label} onChange={(e) => updateSpecification(index, fieldIndex, { label: e.target.value })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label><label className="text-xs font-semibold text-slate-700">Value<input value={field.value} onChange={(e) => updateSpecification(index, fieldIndex, { value: e.target.value })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label></div><div className="mt-2 flex flex-wrap items-center gap-2"><button type="button" onClick={() => moveSpecification(index, fieldIndex, -1)} disabled={fieldIndex === 0} className="rounded border border-slate-200 px-2 py-1 text-xs disabled:opacity-30">Up</button><button type="button" onClick={() => moveSpecification(index, fieldIndex, 1)} disabled={fieldIndex === section.fields.length - 1} className="rounded border border-slate-200 px-2 py-1 text-xs disabled:opacity-30">Down</button><label className="flex items-center gap-2 text-xs font-semibold"><input type="checkbox" checked={field.enabled !== false} onChange={(e) => updateSpecification(index, fieldIndex, { enabled: e.target.checked })} /> Enabled</label><button type="button" onClick={() => removeSpecification(index, fieldIndex)} className="rounded border border-rose-200 px-2 py-1 text-xs font-bold text-rose-700">Remove</button></div></div>)}</div></div> : null}{section.type === 'social_proof' ? <div className="mt-3 rounded-lg bg-slate-50 p-3"><div className="flex items-center justify-between gap-2"><p className="text-xs font-bold uppercase tracking-[0.12em] text-slate-500">Customer reviews</p><button type="button" onClick={() => addReview(index)} className="rounded border border-emerald-200 px-2 py-1 text-xs font-bold text-emerald-700">Add review</button></div><p className="mt-1 text-xs text-slate-500">Only publish real, approved customer content.</p><div className="mt-3 space-y-3">{(section.reviews ?? []).map((review, reviewIndex) => <div key={`${section.id}-review-${reviewIndex}`} className="rounded-lg border border-slate-200 bg-white p-3"><div className="grid gap-2 sm:grid-cols-2"><label className="text-xs font-semibold text-slate-700">Customer name<input value={review.name} onChange={(e) => updateReview(index, reviewIndex, { name: e.target.value })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label><label className="text-xs font-semibold text-slate-700">Image URL<input value={review.imageUrl ?? ''} onChange={(e) => updateReview(index, reviewIndex, { imageUrl: e.target.value || undefined })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" placeholder="Optional approved HTTPS URL" /><input type="file" accept="image/jpeg,image/png,image/webp,image/avif" disabled={uploading} onChange={(e) => { const file = e.target.files?.[0]; if (file) void uploadReviewMedia(file, index, reviewIndex); e.currentTarget.value = '' }} className="mt-2 block w-full text-xs" /></label><label className="text-xs font-semibold text-slate-700">Image caption<input value={review.imageCaption ?? ''} onChange={(e) => updateReview(index, reviewIndex, { imageCaption: e.target.value || undefined })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" placeholder="Optional accessible caption" /></label></div><label className="mt-2 block text-xs font-semibold text-slate-700">Review text<textarea value={review.text} onChange={(e) => updateReview(index, reviewIndex, { text: e.target.value })} rows={3} className="mt-1 w-full rounded border border-slate-200 px-2 py-2 text-xs" /></label><div className="mt-2 grid gap-2 sm:grid-cols-3"><label className="text-xs font-semibold text-slate-700">Rating<input type="number" min="1" max="5" value={review.rating ?? ''} onChange={(e) => updateReview(index, reviewIndex, { rating: e.target.value ? Number(e.target.value) : undefined })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label><label className="text-xs font-semibold text-slate-700">Order<input type="number" min="0" value={review.sortOrder ?? reviewIndex} onChange={(e) => updateReview(index, reviewIndex, { sortOrder: Number(e.target.value) || 0 })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label><label className="flex items-end gap-2 text-xs font-semibold text-slate-700"><input type="checkbox" checked={review.enabled !== false} onChange={(e) => updateReview(index, reviewIndex, { enabled: e.target.checked })} /> Enabled</label></div><button type="button" onClick={() => removeReview(index, reviewIndex)} className="mt-2 rounded border border-rose-200 px-2 py-1 text-xs font-bold text-rose-700">Remove review</button></div>)}</div></div> : null}{section.type === 'faq' ? <div className="mt-3 rounded-lg bg-slate-50 p-3"><div className="flex items-center justify-between gap-2"><p className="text-xs font-bold uppercase tracking-[0.12em] text-slate-500">FAQ items</p><button type="button" onClick={() => addFaq(index)} className="rounded border border-emerald-200 px-2 py-1 text-xs font-bold text-emerald-700">Add FAQ</button></div><div className="mt-3 space-y-3">{section.items.map((item, itemIndex) => <div key={`${section.id}-faq-${itemIndex}`} className="rounded-lg border border-slate-200 bg-white p-3"><label className="block text-xs font-semibold text-slate-700">Question<input value={item.question} onChange={(e) => updateFaq(index, itemIndex, { question: e.target.value })} className="mt-1 h-9 w-full rounded border border-slate-200 px-2 text-xs" /></label><label className="mt-2 block text-xs font-semibold text-slate-700">Answer<textarea value={item.answer} onChange={(e) => updateFaq(index, itemIndex, { answer: e.target.value })} rows={3} className="mt-1 w-full rounded border border-slate-200 px-2 py-2 text-xs" /></label><div className="mt-2 flex flex-wrap items-center gap-3"><label className="text-xs font-semibold text-slate-700">Order<input type="number" min="0" value={item.sortOrder ?? itemIndex} onChange={(e) => updateFaq(index, itemIndex, { sortOrder: Number(e.target.value) || 0 })} className="ml-1 h-8 w-16 rounded border border-slate-200 px-2 text-xs" /></label><label className="flex items-center gap-2 text-xs font-semibold text-slate-700"><input type="checkbox" checked={item.enabled !== false} onChange={(e) => updateFaq(index, itemIndex, { enabled: e.target.checked })} /> Enabled</label><button type="button" onClick={() => removeFaq(index, itemIndex)} className="rounded border border-rose-200 px-2 py-1 text-xs font-bold text-rose-700">Remove</button></div></div>)}</div></div> : null}<div className="mt-2 flex flex-wrap gap-1.5"><button type="button" onClick={() => moveSection(index, -1)} disabled={index === 0} className="rounded border border-slate-200 p-1.5 text-slate-500 disabled:opacity-30" aria-label="Move section up"><ChevronUp className="h-3.5 w-3.5" /></button><button type="button" onClick={() => moveSection(index, 1)} disabled={index === form.sections.length - 1} className="rounded border border-slate-200 p-1.5 text-slate-500 disabled:opacity-30" aria-label="Move section down"><ChevronDown className="h-3.5 w-3.5" /></button><button type="button" onClick={() => duplicateSection(index)} className="rounded border border-slate-200 px-2 py-1 text-xs font-bold text-slate-600">Duplicate</button><button type="button" onClick={() => removeSection(index)} className="rounded border border-rose-200 px-2 py-1 text-xs font-bold text-rose-700">Delete</button></div></div>)}</div><div className="mt-3"><label className="text-xs font-bold uppercase tracking-[0.12em] text-slate-500">Add section</label><select defaultValue="" onChange={(e) => { if (e.target.value) { addSection(e.target.value as LandingSection['type']); e.target.value = '' } }} className="mt-1 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm"><option value="">Choose a section type…</option>{['announcement', 'hero', 'features', 'product', 'offer', 'product_gallery', 'benefits', 'specifications', 'variant_selector', 'quantity_selector', 'delivery_info', 'warranty', 'social_proof', 'image_text', 'comparison', 'related_products', 'sticky_mobile_cta', 'countdown', 'order', 'trust', 'faq', 'rich_text', 'cta'].map((type) => <option key={type} value={type}>{type.replaceAll('_', ' ')}</option>)}</select></div></div>
        <details className="rounded-xl border border-slate-200 bg-white p-3"><summary className="cursor-pointer text-sm font-bold text-slate-800">Advanced section settings</summary><textarea value={sectionsText} onChange={(e) => { setSectionsText(e.target.value); try { setForm((current) => ({ ...current, sections: parseSections(e.target.value) })) } catch {} }} rows={14} className="mt-3 w-full rounded-lg border border-slate-200 bg-slate-950 p-3 font-mono text-xs text-emerald-100" aria-describedby="section-help" /><span id="section-help" className="mt-1 block text-xs font-normal text-slate-500">Use this only for advanced field editing. The server validates all section types, IDs, links, text bounds, schedules, and product requirements.</span></details>
        {feedback ? <p className="rounded-lg bg-amber-50 p-3 text-sm font-semibold text-amber-800" role="status">{feedback}</p> : null}<button type="button" disabled={busy} onClick={submit} className="inline-flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-emerald-700 px-4 text-sm font-bold text-white hover:bg-emerald-800 disabled:cursor-not-allowed disabled:opacity-60"><Save className="h-4 w-4" />{busy ? 'Saving…' : editingPage ? 'Save changes' : 'Save draft'}</button>
      </div>
    </section>
  </div>
}
